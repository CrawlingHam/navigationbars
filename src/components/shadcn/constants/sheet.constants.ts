import { Root, Trigger, Portal, Close } from "@radix-ui/react-dialog";

const SheetTrigger = Trigger;
const SheetPortal = Portal;
const SheetClose = Close;
const Sheet = Root;

export { SheetTrigger, SheetPortal, SheetClose, Sheet };
