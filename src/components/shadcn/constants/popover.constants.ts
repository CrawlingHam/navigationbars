import { Root, Anchor, Trigger } from "@radix-ui/react-popover";

const PopoverTrigger = Trigger;
const PopoverAnchor = Anchor;
const Popover = Root;

export { PopoverTrigger, PopoverAnchor, Popover };
